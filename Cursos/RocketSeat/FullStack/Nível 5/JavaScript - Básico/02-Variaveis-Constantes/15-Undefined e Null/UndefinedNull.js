//Undefined (indefinido)

// É o tipo de dado que o JS atribui para uma variável que ainda não tem nenhum valor

let emptiness 
console.log("O valor é: ", emptiness) // Retorna undefined pois não atribuimos nada a ela


// Null

// É o tipo de dado que o JS atribui para uma variável para dizer que ela é vazia, que não tem nada dentro do objeto
let empty = null
console.log("O valor é: ", empty) 
