#include <stdio.h>

//somente serão executados código dentro do main, fora do main o computador apenas nao vai considerar/ler o código
int main()
{
    printf ("Hello World");

    return 0; // o return é padrão do código, não tendo funcionalidade nenhuma, apenas de rotina, porém é ele quem finaliza o código (então se você colocar antes de alguma instrução, a mesma não vai ler ela), aparecendo apenas uma vez no código todo e por padrão é dado como 0 se vc nao inseri-lo.

}

//Professor está trabalhando apenas os erros, e seus impactos, e como isso pode ser 